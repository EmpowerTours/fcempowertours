/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',  // For Railway container
  experimental: { serverComponentsExternalPackages: ['@web3modal/wagmi'] },
};
export default nextConfig;
