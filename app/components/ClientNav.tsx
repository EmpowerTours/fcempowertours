'use client';

/**
 * ClientNav - DISABLED
 * All navigation now happens through the Oracle chat interface.
 * Users interact with the Oracle to access all features.
 */
export default function ClientNav() {
  // Oracle-only experience - no traditional navigation
  return null;
}
